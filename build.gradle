buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath 'com.mparticle:android-kit-plugin:' + project.version
    }
}

apply plugin: 'com.mparticle.kit'

android {
    lintOptions {
        abortOnError false
    }
    defaultConfig {
        minSdkVersion 14
    }

}

dependencies {
    compile 'com.stepleaderdigital.reveal:reveal:1.3.29'
}
